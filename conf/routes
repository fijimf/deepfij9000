# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
GET     /other                      controllers.Application.other
GET     /team                      controllers.Application.team
GET     /scrape                      controllers.Loader.loadReferenceData
GET     /scrapeg                      controllers.Loader.loadSingleTeam
GET     /aliases/:key                      controllers.MongoLoader.findByAlias(key)
# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
